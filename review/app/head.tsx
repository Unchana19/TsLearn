import { FC } from "react";
import AppHead from "../components/AppHead";

interface Props {}

const head: FC<Props> = (props): JSX.Element => {
  return (
    <AppHead />
  );
};

export default head;
